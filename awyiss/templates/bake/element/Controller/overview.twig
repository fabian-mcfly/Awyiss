
	/**
	 * Overview method
	 *
	 * @throws \Exception
	 */
	public function overview(): void {
		$this->Authorization->ensure('read');


		$query = $this->getOverviewQuery();
		${{ pluralName }} = $this->paginate($query);

		$this->set([
			'{{ pluralName }}' => ${{ pluralName }},
			'attributes' => $this->{{ currentModelName }}->getAttributes(),
		]);
	}
