
	/**
	 * Add method
	 *
	 * @return void
	 * @throws \Exception
	 */
	public function add(): void {
		$this->Authorization->ensure('create');

		${{ singularName }} = $this->{{ currentModelName }}->newDefaultEntity();

		if ($this->request->is('post')) {
			$this->save(${{ singularName }});
		}

		$this->set([
			'{{ singularName }}' => ${{ singularName }},
		]);
	}
