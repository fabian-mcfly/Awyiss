{#
@var ao_menuEntry \Awyiss\Model\Entity\MenuEntry
@var ao_threadedMenuEntries \Cake\Collection\Iterator\TreeIterator
@var as_languageRealm string
@var _view \Awyiss\View\BackendView
#}

<div class="MenuEntries Form Edit">
	{{ helper_Form_create(ao_menuEntry, {method: 'post', idPrefix: 'menuEntry'}) }}

	{{ helper_Form_checkbox('save_as_copy', {id: 'SaveAsCopy', value: true}) }}

	{% block headlineEdit %}
		<h1>{{ __('headline_edit') }}</h1>
	{% endblock %}

	{% block formEditButtons %}
		<label for="SaveAsCopy">{{ __('as_copy') }}</label>
		{{ helper_Form_button(__('save')) }}
		{{ helper_Form_button(__('save_close'), {name: 'submit', value: 'submit_close'}) }}
	{% endblock %}

	{% block formEdit %}
		{% include('@Backend/MenuEntries/form.twig') %}
	{% endblock %}

	{% block formEditButtonsBottom %}
		{{ block('formEditButtons') }}
	{% endblock %}

	{{ helper_Form_end() -}}
</div>