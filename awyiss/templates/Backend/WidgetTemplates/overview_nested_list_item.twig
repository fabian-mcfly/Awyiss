{#
@var entity \Awyiss\Model\Entity
@var childrenList string
@var settings array
#}

{% extends '@Backend/element/overview/nested_list_item.twig' %}

{% block nestedListItemDataDefaultFields %}
	<li class="DataItem DataItem-UsedForWidgets">
		<span class="Label">{{ __('used_for_widgets') }}:</span>
		{{ entity.usedForWidgets }}
	</li>
{% endblock %}

{% block nestedListActionsDelete %}
	{% if entity.usedForWidgets == 0 %}
		{{ parent() }}
	{% else %}
		{% if AuthorizationHelper.isAccessible('delete') %}
			<!--suppress HtmlUnknownAttribute -->
			<a class="Button Button-Small Button-Delete" disabled>{{ __('delete') }}</a>
		{% endif %}
	{% endif %}
{% endblock %}