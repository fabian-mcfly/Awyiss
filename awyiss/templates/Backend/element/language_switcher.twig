{#
@var language \Awyiss\Model\Entity\Language
@var languages \Awyiss\Model\Entity\Language[]
#}

{% set currentAction = _view.getRequest().getParam('action') %}
{% set languages = languages|filter(language => language.active) %}

<div class="LanguageSwitcher">
	<button class="Button LanguageSwitcherLabel">
		<img src="{{ UrlHelper.build('awyiss/assets/img/countries/' ~ currentLanguage.shortcode ~'.svg') }}" alt="{{ currentLanguage.title }}">
		{{ currentLanguage.title }}
	</button>

	<ul class="Languages">
		{% for language in languages %}
			<li class="Language{% if language.shortcode == currentLanguage.shortcode %} Active{% endif %}">
				{% set icon %}
					<img src="{{ UrlHelper.build('awyiss/assets/img/countries/' ~ language.shortcode ~'.svg') }}" alt="{{ language.title }}">
				{% endset %}
				{{ HtmlHelper.link(icon ~ language.title, {lang: language.shortcode, action: currentAction == 'edit' ? 'overview' : null}, {
					escape: false,
					title: language.title,
				}) }}
			</li>
		{% endfor %}
	</ul>
</div>