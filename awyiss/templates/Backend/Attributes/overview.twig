{#
@var attributes \Awyiss\Model\Entity\Attribute[]
@var attributesGroupedByFieldset array
@var attributeScopes array
@var _view \Awyiss\View\BackendView
@var CategoriesHelper \Awyiss\View\Helper\CategoriesHelper
#}

<div class="Attributes Overview">
	{% block headlineOverview %}
		<h1>{{ __('headline_overview') }}</h1>
	{% endblock %}

	{% block buttonArea %}
		<div class="ButtonArea">
			{% block categoriesFilterOverview %}
				{{ CategoriesHelper.filter('scope') }}
			{% endblock %}

			{% block overviewButtons %}
				{% block overviewButtonSaveSystemOrder %}
					{% if paginated == false %}
						<button class="Button Button-SaveSystemOrder" disabled>{{ __d('system', 'save_system_order') }}</button>
					{% endif %}
				{% endblock %}

				{% block overviewButtonAdd %}
					{% if AuthorizationHelper.isAccessible('create') %}
						{{ HtmlHelper.link(__('create'), {action: 'add', scope: selectedScope}, {class: 'Button Button-Add'}) }}
					{% endif %}
				{% endblock %}

				{% block overviewButtonFilter %}
					{{ element('search/filter_button', {scope: 'attributes'}) }}
				{% endblock %}

				{% block listControlOverviewListPaginated %}
					{{ HtmlHelper.link(__('list_paginated'), {action: 'userConfiguration', identifier: 'paginate.enabled', value: 'true'}, {
						class: 'Button Button-Small Button-ListView Button-ListView-PaginatedList',
						disabled: localConfig.paginate.enabled == true
					}) }}
				{% endblock %}

				{% block listControlOverviewListNested %}
					{{ HtmlHelper.link(__('list_nested'), {action: 'userConfiguration', identifier: 'paginate.enabled', value: 'false'}, {
						class: 'Button Button-Small Button-ListView Button-ListView-NestedList',
						disabled: localConfig.paginate.enabled == false
					}) }}
				{% endblock %}
			{% endblock %}
		</div>
	{% endblock %}

	{% if paginated %}
		{% block paginatedOverview %}
			{% include '@Backend/Attributes/overview_paginated_list.twig' with _context|merge({records: attributes.items().toArray()}) %}
		{% endblock %}
	{% else %}
		{% block fieldsetsOverview %}
			{% for fieldset in availableFieldsets %}
				{% block fieldsetOverview %}
					<fieldset class="Overview-Fieldset Overview-Fieldset-{{ (fieldset ?: 'undefined')|camelize }}">
						{% if fieldset is not empty %}
							<legend>{{ __('fieldset_' ~ fieldset) }}</legend>
						{% endif %}

						{% block fieldsetOverviewInner %}
							{% block listOverview %}
								{% from '@Backend/macro/nested_list.twig' import nestedList %}
								{{ nestedList(attributesGroupedByFieldset[ fieldset ], _context|merge({
									data: {
										nestable: false,
										fieldset: fieldset,
										sortable: _filter['attributes']['active'] == false,
									},
									displayedFields: localConfig.overview.displayedFields,
									identifier: 'Attributes',
									template: '@Backend/Attributes/overview_nested_list_item.twig',
								})) }}
							{% endblock %}
						{% endblock %}
					</fieldset>
				{% endblock %}
			{% endfor %}
		{% endblock %}
	{% endif %}

	{% block buttonAreaBottom %}
		<div class="ButtonArea ButtonArea-Bottom">
			{{ block('categoriesFilterOverview') }}

			{{ block('overviewButtons') }}
		</div>
	{% endblock %}

	{% block listFilter %}
		{{ element('search/filter', {scope: 'attributes'}) }}
	{% endblock %}
</div>
