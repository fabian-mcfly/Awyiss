{% layout 'error' %}

{% if staticCall('\\Cake\\Core\\Configure', 'read', 'debug') %}
	{% layout 'dev_error' %}

	{% do _view.assign('title', message) %}
	{% do _view.assign('templateName', 'Backend/error500.twig') %}

	{% do _view.start('file') %}

	{% if error.queryString %}
		<p class="notice">
			<strong>SQL Query: </strong>
			{{ error.queryString|escape }}
		</p>
	{% endif %}

	{% if error.params %}
		<strong>SQL Query Params: </strong>
		{{ staticCall('\\Cake\\Error\\Debugger', 'dump', error.params)|raw }}
	{% endif %}

	{% if error is instanceOf('Error') or error is instanceOf('Exception') %}
		<strong>Error in: </strong>
		{{ '%s, line %s'|format(error.getFile|replace({(constant('ROOT')): 'ROOT'}), error.getLine) }}
	{% endif %}

	{{ element('auto_table_warning') }}

	{% do _view.end()%}

{% endif %}

<h2>{{ __('An Internal Error Has Occurred') }}</h2>
<p class="error">
	<strong>{{ __('Error') }}: </strong>
	{{ message|escape }}
</p>
