{#

@var ao_pageTemplate Awyiss\Model\Entity\PageTemplate
@var _view \Awyiss\View\BackendView

#}

<div class="PageTemplates Form Add">
	{{ helper_Form_create(ao_pageTemplate, {'method': 'post', 'idPrefix': 'pageTemplate'}) }}
	
	<fieldset>
		<legend>{{ __('::headline_add') }}</legend>
		{{ helper_Form_control('title') }}
		{{ helper_Form_control('filename') }}
		{{ helper_Form_control('contentareas') }}
		{{ helper_Form_control('page_roles_id', {'options': ao_pageRoles}) }}
		{{ helper_Form_control('active') }}
		{{ helper_Form_control('system_order') }}
	</fieldset>
	
	{{ helper_Form_button(__('::save')) }}
	{{ helper_Form_button(__('::save_close')) }}
	{{ helper_Form_end() }}
	
</div>