<!DOCTYPE html>
<html lang="en">
	<head>
		{{ helper_Html_charset() }}
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>
			{{ fetch('title') }}
		</title>
		{{ helper_Html_meta('icon') }}

		<link href="https://fonts.googleapis.com/css?family=Lato:400,700" rel="stylesheet">

		{{ helper_Html_css(['normalize.min', 'milligram.min', 'cake']) }}

		{{ fetch('meta') }}
		{{ fetch('css') }}
		{{ fetch('script') }}
	</head>
	<body>
		{% block sidebar %}
			<aside id="Sidebar">
				<a href="{{ helper_Url_build({controller: 'dashboard'}) }}" class="Title">Aw<span>Yiss</span></a>

				{% block systemMenu %}
					<ul class="Menu-System">
						{% if helper_Access_scopeIsAccessible('Pages', NULL, ['read']) %}
							<li>{{ helper_Html_link(__('pages::titleMenu'), {controller: 'Pages'}) }}</li>
						{% endif %}
						{% if helper_Access_scopeIsAccessible('ContentTemplates', NULL, ['read']) %}
							<li>{{ helper_Html_link(__('contentTemplates::titleMenu'), {controller: 'ContentTemplates'}) }}</li>
						{% endif %}

						{% if helper_Access_scopeIsAccessible('Languages', NULL, ['read']) %}
							<li>{{ helper_Html_link(__('languages::titleMenu'), {controller: 'Languages'}) }}</li>
						{% endif %}

						{% if helper_Access_scopeIsAccessible('PageRoles', NULL, ['read']) %}
							<li>{{ helper_Html_link(__('pageRoles::titleMenu'), {controller: 'PageRoles'}) }}</li>
						{% endif %}

						{% if helper_Access_scopeIsAccessible('PageTemplates', NULL, ['read']) %}
							<li>{{ helper_Html_link(__('pageTemplates::titleMenu'), {controller: 'PageTemplates'}) }}</li>
						{% endif %}

						{% if helper_Access_scopeIsAccessible('Configuration', NULL, ['read']) %}
							<li>{{ helper_Html_link(__('configuration::titleMenu'), {controller: 'Configuration'}) }}</li>
						{% endif %}

						{% if helper_Access_scopeIsAccessible('Users', NULL, ['read']) %}
							<li>{{ helper_Html_link(__('users::titleMenu'), {controller: 'Users'}) }}</li>
						{% endif %}

						{% if helper_Access_scopeIsAccessible('Usergroups', NULL, ['read']) %}
							<li>{{ helper_Html_link(__('usergroups::titleMenu'), {controller: 'Usergroups'}) }}</li>
						{% endif %}

						{% if helper_Access_scopeIsAccessible('Attributes', NULL, ['read']) %}
							<li>{{ helper_Html_link(__('attributes::titleMenu'), {controller: 'Attributes'}) }}</li>
						{% endif %}
					</ul>
				{% endblock %}
			</aside>
		{% endblock %}
		{% block body %}
			<div id="Body">
				{% block content %}
					<main id="Content">
						{{ helper_Flash_render() }}
						{{ fetch('content') }}
					</main>
				{% endblock %}
				{% block footer %}
					<footer>
						{{ helper_Html_link(__('users::logout'), {controller: 'users', action: 'logout'}) }}
					</footer>
				{% endblock %}
			</div>
		{% endblock %}
	</body>
</html>
