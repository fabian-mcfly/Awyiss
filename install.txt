// Cake cli ausführbar machen
chmod +x bin/cake


// Cronjob
sudo crontab -e -u www-data
*/10 * * * * cd /var/www/cms && bin/cake queue run -q -g general >> /var/www/cms/logs/cron.log 2>&1
*/1 * * * * cd /var/www/cms && bin/cake media convert_files --include-avif --include-webp -q


// Annotations
sudo runuser -u www-data -- bin/cake generate phpstorm


// Medienzeugs
sudo runuser -u www-data -- bin/cake media convert_files --include-avif --include-webp
sudo runuser -u www-data -- bin/cake media detect_available_commands
sudo runuser -u www-data -- bin/cake media clear_cache


// System installieren
sudo runuser -u www-data -- bin/cake awyiss install
sudo runuser -u www-data -- bin/cake awyiss install --rebuild-symlinks

// AiO Install:
chmod +x bin/cake && bin/cake awyiss install && bin/cake media detect_available_commands


// Backup erstellen
sudo runuser -u www-data -- bin/cake awyiss backup


// Inhalte finden, deren Attribut "Testattribut" vor dem 31.12.2028 liegt
$contents->find('pastTestattribute', new \Cake\I18n\DateTime('2028-12-31 23:59:59'));
// Inhalte finden, deren Attribut "Testattribut" nach dem 31.12.2028 liegt
$contents->find('futureTestattribute', new \Cake\I18n\DateTime('2028-12-31 23:59:59'));


// Seiten finden, deren Startdatum vor dem 14.03.2024 20:15:00 liegt oder die kein Startdatum haben
$pages->find('publishedStartingBefore', new \Cake\I18n\DateTime('2024-03-14 20:15:00'));

// Seiten finden, deren Startdatum vor dem 14.03.2024 20:15:00 liegt
$pages->find('publishedStartingBefore', new \Cake\I18n\DateTime('2024-03-14 20:15:00'), false);

// Seiten finden, deren Startdatum nach dem 14.03.2024 20:15:00 liegt oder die kein Startdatum haben
$pages->find('publishedStartingAfter', new \Cake\I18n\DateTime('2024-03-14 20:15:00'));

// Seiten finden, deren Startdatum nach dem 14.03.2024 20:15:00 liegt
$pages->find('publishedStartingAfter', new \Cake\I18n\DateTime('2024-03-14 20:15:00'), false);

// Seiten finden, deren Enddatum vor dem 14.03.2024 20:15:00 liegt oder die kein Enddatum haben
$pages->find('publishedEndingBefore', new \Cake\I18n\DateTime('2024-03-14 20:15:00'));

// Seiten finden, deren Enddatum vor dem 14.03.2024 20:15:00 liegt
$pages->find('publishedEndingBefore', new \Cake\I18n\DateTime('2024-03-14 20:15:00'), false);

// Seiten finden, deren Enddatum nach dem 14.03.2024 20:15:00 liegt oder die kein Enddatum haben
$pages->find('publishedEndingAfter', new \Cake\I18n\DateTime('2024-03-14 20:15:00'));

// Seiten finden, deren Enddatum nach dem 14.03.2024 20:15:00 liegt
$pages->find('publishedEndingAfter', new \Cake\I18n\DateTime('2024-03-14 20:15:00'), false);


// Seiten finden, die am 14.03.2024 20:15:00 veröffentlicht sind
$pages->find('published',
	at: new \Cake\I18n\DateTime('2024-03-14 20:15:00'),
);


// JS served by nginx?
location ~* /ProgressCheckerWorker\.js$ {
    add_header Service-Worker-Allowed "/";
}

// When updating from 0.2.6 or below, run migration update to switch to the cake_migrations table after the removal of phinx
bin/cake migrations upgrade --dry-run
bin/cake migrations upgrade --drop-tables