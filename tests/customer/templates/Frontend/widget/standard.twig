{#
@var widget \Awyiss\Model\Entity\Content
@var children string
@var page \Awyiss\Model\Entity\Page
@var baseUrl string
@var currentLanguage \Awyiss\Model\Entity\Language
@var currentPath string
@var currentUrl string
@var folder string
@var languages \Awyiss\Model\Entity\Language[]
@var languageShortcode string
@var _view \Awyiss\View\FrontendView
@var AssetHelper \Awyiss\View\Helper\AssetHelper
@var FormHelper \Awyiss\View\Helper\FormHelper
@var HtmlHelper \Cake\View\Helper\HtmlHelper
@var LocaleHelper \Awyiss\View\Helper\LocaleHelper
@var MediaHelper \Awyiss\View\Helper\MediaHelper
@var TimeHelper \Cake\View\Helper\TimeHelper
@var UrlHelper \Awyiss\View\Helper\UrlHelper
@var mediaRenderOptions \Awyiss\Utility\Media\MediaRenderOptions

// In case you need to set a class for the row, you can do it like this:
{%- do _view.setRowClass('FlexRow') -%}

#}

<div class="{{ widget.cssClass }}" {{ widget.data|data_attr|raw }} id="Widget{{ widget.id }}">
	{% if widget.title %}
		{% set tag = widget.titleTag ?? 'div' %}
		<{{ tag }} class="Title" id="{{ widget.title|ucparts(false) }}">
			{{ widget.title|raw }}
		</{{ tag }}>
	{% endif %}

	{% if widget.subtitle %}
		{% set tag = widget.subtitleTag ?? 'div' %}
		<{{ tag }} class="Subtitle" id="{{ widget.subtitle|ucparts(false) }}">
			{{ widget.subtitle|raw }}
		</{{ tag }}>
	{% endif %}

	{% if widget.text %}
		<div class="Text">
			{{ widget.text|raw }}
		</div>
	{% endif %}

	{% if widget.formId %}
		{{ form(widget.formId) }}
	{% endif %}

	{% if children %}
		<div class="Children">
			{{ children|raw }}
		</div>
	{% endif %}
</div>
